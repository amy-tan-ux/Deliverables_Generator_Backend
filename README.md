# Deliverables Generator Flask Backend
This is the backend code for the deliverables generator endpoint

# .env var required
AZURE_OPENAI_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=
KEY_VAULT_URL =
PORT=
SESSION_SECRET_KEY=

# How to Run
python -m pip install -r requirements.txt
flask run
